pipeline {
    agent any

    tools {
        maven 'maven3'
    }

    stages {
        stage("Build") {
            steps {
                sh 'mvn package -DskipTests'  // Build the app
            }
        }

        stage("Create and Push") {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub', 
                    usernameVariable: 'USER', 
                    passwordVariable: 'PASS'
                )]) {
                    sh 'docker build -t inde249/test-java:my_app .'  // Build Docker image
                    sh 'echo $PASS | docker login -u $USER --password-stdin'  // Login to Docker registry
                    sh 'docker push inde249/test-java:my_app'  // Push the image to Docker Hub
                }
            }
        }

        stage("Deploy") {
            steps {
                echo "Deploying app"  // Placeholder for deployment logic
            }
        }
    }
  post {
        success {
            // Send an email on success
            mail to: 'inder.23.edu@gmail.com',  // Change to your email address
                 subject: "Pipeline Build Success",
                 body: "The build was successful, and the Docker image was pushed to the registry."
        }

        failure {
            // Send an email on failure
            mail to: 'inder.23.edu@gmail.com',  // Change to your email address
                 subject: "Pipeline Build Failure",
                 body: "The build failed. Please check the Jenkins logs for more information."
        }

        always {
            // Send an email regardless of the build result (optional)
            mail to: inder.23.edu@gmail.com,  // Change to your email address
                 subject: "Pipeline Execution Complete",
                 body: "The pipeline execution has completed. Check the logs for more details."
        }
    }

    }

